{
  "name": "@frame-sight/desktop",
  "private": true,
  "version": "0.1.0",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "dev": "node ../../scripts/wait-for.mjs http://localhost:5173 && tsc -b && tsc -p tsconfig.preload.json && mv dist/preload.js dist/preload.cjs && VITE_DEV_SERVER_URL=http://localhost:5173 electron .",
    "build": "tsc -b && tsc -p tsconfig.preload.json && mv dist/preload.js dist/preload.cjs",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.preload.json --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.637.0",
    "@aws-sdk/s3-request-presigner": "^3.637.0",
    "electron-store": "^10.0.0",
    "fluent-ffmpeg": "^2.1.3"
  },
  "devDependencies": {
    "@types/fluent-ffmpeg": "^2.1.27",
    "electron": "^41.0.3",
    "typescript": "^5.5.4"
  }
}
